# NEO — 프로젝트 규칙

해외 수출 규제 대응 PWA. 복잡한 해외 법률 변화를 기업별 영향과 **실행 항목**으로 변환한다.

핵심 명제: **"이 법이 바뀌었다"가 아니라 "그래서 당신은 이걸 해야 한다"를 판다.**

---

## 명세 문서

| 파일 | 내용 |
|---|---|
| `docs/build-plan.md` | **작업 계획서. §10의 12단계를 순서대로 진행한다.** |
| `docs/PRD-v2.md` | 무엇을 만드는가 — IA, 데이터 모델, 성공 기준 |
| `design/NEO Toss Reform.dc.html` | **시각 원본.** 토큰 · 부품 · 8화면(S1~S6, 셋업 2/4·4/4) · 다크. 인라인 스타일에 실제 값이 들어 있다 |

구현 중 값이 헷갈리면 **`design/NEO Toss Reform.dc.html`이 최종 진실**이다. 문서보다 우선한다.
`docs/design-prompts.md` · `docs/component-mapping.md`는 이전 디자인(다크 단일 · 무라운드 · 한자 마커) 기준이라 **시각 값의 근거로 쓰지 않는다.** 목업과 앱이 일부러 다른 곳은 `design/README.md`에 적었다.

---

## 디자인 규칙 — TDS(토스 디자인 시스템) 기반

1. **색은 토큰으로만.** `app/globals.css`의 `--tds-*` · `--risk-*` 변수를 쓴다. 색 리터럴 하드코딩 금지. 필요한 토큰이 없으면 globals.css에 이름을 붙여 더한다.
2. **라이트가 기준, 다크는 기기 설정을 따른다.** `prefers-color-scheme` 미디어 쿼리가 같은 토큰 이름을 덮는다. 컴포넌트 안에서 테마를 분기하지 않고, 앱 안 테마 스위치도 없다. 화면을 고치면 두 테마 모두 확인한다.
3. **라운드 사다리.** 배지·체크박스 6 · 입력칸·버튼 M/S 12 · 아이콘 타일·콜아웃 14 · 버튼 XL·작은 카드 16 · 카드 20 · 시트·탭바 24 · 칩·진행바·점 999. 사다리 밖 값을 새로 만들지 않는다.
4. **그림자는 떠 있는 표면에만.** 탭바 · 시트 · 지도 마커 · 설치 카드 · 세그먼트 선택 칸. **카드는 그림자 없이** 1px `--tds-line-subtle` 테두리로 선다. 카드 안 행 구분은 1px `--tds-line-default`.
5. **브랜드 파랑은 주 행동과 진행에만.** 주 CTA · 진행 막대 · 체크 · 선택 상태 · 텍스트 버튼. 위험도는 **washed 배지**(연한 면 + 같은 계열 진한 글자, `types/neo.ts`의 `TONE_COLOR`)로 말한다.
6. **폰트는 Pretendard 하나.** 400·500·600·700을 `public/fonts/`에 자체 호스팅한다. CDN 금지. 되풀이되는 조합은 globals.css의 `.t-*` 클래스를 쓴다.
7. **아이콘은 `components/Icon.tsx`의 인라인 SVG만.** Lucide 격자(24, 둥근 끝), `currentColor`. 아이콘 패키지를 설치하지 않는다. **이모지 금지** — 국기 포함. 국가는 `VN` / `ID` / `TH` 두 글자.
8. **UI 문구는 해요체.** 합쇼체(…합니다 / …십시오)와 단정형(…다)을 UI 문구에 쓰지 않는다. 영문 대문자는 위험도 배지(`CRITICAL`)와 법령 번호만. `data/`의 법령 원문(제목·변경 내용·액션 문구)은 이 규칙 밖이다 — 경과규정만 해요체로 옮겼다.
9. **스켈레톤에 shimmer 금지.** opacity 펄스만. 모션은 120 / 200ms, 바운스 없음.
10. **기능 없는 컨트롤을 그리지 않는다.** 디자인에 있어도 누를 곳이 없으면 뺀다 — S2·S5 우상단 필터 아이콘을 뺀 이유다.

상태는 한자 마커(施留豫)가 아니라 **배지 글자**가 말한다 — `보류` · `시행중` · 카운트다운(`lib/derive.ts`의 `lawBadge`). 위험도는 법령 행 왼쪽 아이콘 타일의 색이다.

---

## 작업 원칙

**생각하고 코딩한다.** 가정은 명시한다. 해석이 갈리면 임의로 고르지 말고 묻는다. 막히면 멈추고 무엇이 불명확한지 말한다.

**단순함이 먼저다.** 요청하지 않은 기능을 만들지 않는다. 일회성 코드에 추상화를 넣지 않는다. 추상화는 실제로 2번 이상 반복될 때만. 200줄을 썼는데 50줄로 되면 다시 쓴다.

**외과적으로 고친다.** 요청과 무관한 코드·주석·포맷을 "개선"하지 않는다. 기존 스타일을 따른다. 내 변경이 만든 미사용 import만 정리한다.

**검증으로 굴린다.** `docs/build-plan.md` §10의 각 단계에는 검증 기준이 붙어 있다. **통과해야 다음으로 넘어간다.** 실패하면 넘어가지 말고 멈춰서 보고한다.

---

## 기술 결정 (확정)

| 항목 | 값 |
|---|---|
| 스택 | Next.js App Router + TypeScript + Tailwind + shadcn |
| 데이터 | 정적 JSON + localStorage. 백엔드 없음 |
| 지오그래피 | `design/neo-dots.js` 포팅. **cobe·three.js·외부 지도 라이브러리 금지** |
| PWA | 서비스워커를 직접 쓴다. Serwist·next-pwa 쓰지 않는다 |
| 배포 | Vercel |

**`neo-dots.js` 포팅 시 반드시**: CDN 의존(unpkg d3, topojson, jsdelivr world-atlas)을 전부 제거하고 `d3-geo`·`topojson-client`를 npm으로, `countries-110m.json`을 `public/geo/`에 정적으로 포함한다. 오프라인 PWA에서 CDN 의존은 치명적이다.

---

## 기준 날짜

목 데이터의 D-Day는 **2026-09-03** 기준으로 계산되어 있다.
`data/meta.json`의 `referenceDate`를 쓰고 `new Date()`를 쓰지 않는다.
실제 시각을 쓰면 D-Day가 디자인 산출물과 어긋난다.
